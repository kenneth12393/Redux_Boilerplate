import { DEMO_ACTION } from "./types" //import all action types neccessary here

export const randomAction = () => dispatch => {
    //this is where the code for the action goes
}

//there can also be multiple actions